#include<stdio.h>
int main()
{
    int area, l,b;
    printf("enter the length of rect:");
    scanf("%d",&l);
    printf("enter the breadth of rectangle:");
    scanf("%d",&b);
    area=l*b;
    printf("area of rectange with length %d and breadth %d is : %d",l,b,area);


    return 0;
}
