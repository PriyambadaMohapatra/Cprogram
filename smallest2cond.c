#include <stdio.h>
int main()
{
    int a,b;
    printf("enter two numbers to find the smallest:");
    scanf("%d""%d",&a,&b);
    ((a<b))?
    printf("%d is smallest ",a):
    printf("%d is smallest",b);
    return 0;
}
